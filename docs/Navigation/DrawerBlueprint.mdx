export const meta = { category: "Navigation" }

# Drawer (Blueprint)

Blueprint Drawer recreated based on Figma node `308:55`, with layout, size, spacing, typography, and colors strictly aligned to Figma:
- Size: width `375px`, top border radius `16px 16px 0 0`
- Layout: vertical stack, `gap: 8px`, `padding: 16px 0 40px`
- Title: `Fira Sans 20/Bold`, text color `#000000`
- List item: `Fira Sans 18/Regular`, selected background `#F3F3F3`
- Icon: `24×24` on the left (uses `Plus` to approximate Figma `Icon/Add`)

## Preview

<Demo title="Figma Parity Preview" description="Visual parity with Figma Drawer (Blueprint)">
  <DrawerBlueprint />
</Demo>

## Usage

```tsx
import DrawerBlueprint from "@/content/demos/Navigation/DrawerBlueprint"

export default function Page() {
  return <DrawerBlueprint />
}
```

## Notes

- Typography uses `Fira Sans` via CSS; ensure the font is available for offline parity.
- Icon uses `lucide-react` `Plus` to approximate Figma `Icon/Add`; for pixel-perfect parity, replace with the SVG exported from Figma.
- This component demonstrates a static structure (Blueprint) and does not include interactions (toggle, animations, backdrop click-to-close, etc.). Extend from this baseline for a fully interactive version.